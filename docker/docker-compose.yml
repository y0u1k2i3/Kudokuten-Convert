version: "3.9"
services:
  frontend:
    build:
      context: ..
      dockerfile: docker/frontend.dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ..
      dockerfile: docker/backend.dockerfile
    ports:
      - "8000:8000"
